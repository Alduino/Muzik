{
    "name": "@muzik/root",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "preinstall": "npx only-allow pnpm",
        "format": "prettier . --write"
    },
    "devDependencies": {
        "prettier": "^2.2.1"
    }
}
